import React from 'react';
import 'Styles/Tools/Fallback.css';

const Fallback: React.FC = () => {
  return (
    <div className='fallback-container'>
      <p>Loading...</p>
    </div>
  );
};

export default Fallback;
